# Avendish addon CI - reusable workflow
#
# Builds an avendish-based addon for any subset of the 12 supported
# backends across every OS/arch combination each backend supports, packages
# every produced artifact into an end-user-friendly zip, and publishes the
# zips to a single rolling "continuous" GitHub prerelease on every push to
# main / master.
#
# Usage in a caller repository (.github/workflows/builds.yaml):
#
#   name: Build
#   on:
#     push:
#       branches: [main, master]
#     pull_request:
#   concurrency:
#     group: ${{ github.workflow }}-${{ github.ref }}
#     cancel-in-progress: true
#   jobs:
#     build:
#       uses: ossia/actions/.github/workflows/avnd-addon.yml@master
#       secrets: inherit
#       with:
#         pd: true
#         vst3: true
#         clap: true
#
# For the score backend, use the dedicated `score-addon.yml` reusable
# workflow (different build orchestration — requires a score source/SDK
# checkout). Run both workflows side-by-side in a caller's `builds.yaml`
# if the addon ships both as a score plugin and as standalone backends.
#
# Composite vs per-OS artifacts:
#   Backends with `*-composite` in their artifact name ship as a single
#   cross-platform archive (max, pd, godot). The matching per-OS
#   artifacts are still produced for users who want to inspect
#   individual platform builds, but the canonical user-facing download
#   is the composite zip.
#
# Inputs (all booleans, default false - flip to true to enable that backend):
#   pd             Pure Data external      linux/x86_64, macos/arm64, windows/x86_64
#   max            Max/MSP external                     macos/arm64, windows/x86_64
#   vst3           VST3 plugin             linux/x86_64, macos/arm64, windows/x86_64
#   clap           CLAP plugin             linux/x86_64, macos/arm64, windows/x86_64
#   vintage        VintageDSP / LV2-like                macos/arm64, windows/x86_64
#   touchdesigner  TouchDesigner CHOP                   macos/arm64, windows/x86_64
#   python         Python module           linux/x86_64, macos/arm64, windows/x86_64
#   godot          Godot GDExtension       linux/x86_64, macos/arm64, windows/x86_64
#   standalone     standalone executable   linux/x86_64, macos/arm64, windows/x86_64
#   gstreamer      GStreamer plugin        linux/x86_64, macos/arm64
#   wasm           WebAssembly (emscripten) linux/x86_64
#
# Secrets (all optional - signing / notarization is a no-op without them):
#   MAC_CERT_B64            Base64-encoded P12 certificate
#   MAC_CERT_PASSWORD       P12 password
#   APPLE_TEAM_ID           Apple Developer Team ID
#   APPLE_ID                Apple ID for notarytool
#   APPLE_ALTOOL_PASSWORD   App-specific notarization password
#   WIN_CERT_B64            Base64-encoded PFX certificate
#   WIN_CERT_PASSWORD       PFX password

name: Avendish addon CI

on:
  workflow_call:
    inputs:
      pd:
        description: 'Build Pure Data external backend'
        required: false
        type: boolean
        default: false
      max:
        description: 'Build Max/MSP external backend'
        required: false
        type: boolean
        default: false
      vst3:
        description: 'Build VST3 backend'
        required: false
        type: boolean
        default: false
      clap:
        description: 'Build CLAP backend'
        required: false
        type: boolean
        default: false
      vintage:
        description: 'Build VintageDSP backend'
        required: false
        type: boolean
        default: false
      touchdesigner:
        description: 'Build TouchDesigner backend'
        required: false
        type: boolean
        default: false
      python:
        description: 'Build Python backend'
        required: false
        type: boolean
        default: false
      godot:
        description: 'Build Godot GDExtension backend'
        required: false
        type: boolean
        default: false
      standalone:
        description: 'Build standalone executable backend'
        required: false
        type: boolean
        default: false
      gstreamer:
        description: 'Build GStreamer backend'
        required: false
        type: boolean
        default: false
      wasm:
        description: 'Build WebAssembly (emscripten) backend'
        required: false
        type: boolean
        default: false
      targets:
        description: >-
          Space-separated os/arch triples to build (e.g.
          "linux/x86_64 windows/x86_64"). Empty = every platform.
        required: false
        default: ''
        type: string
    secrets:
      MAC_CERT_B64:
        required: false
      MAC_CERT_PASSWORD:
        required: false
      APPLE_TEAM_ID:
        required: false
      APPLE_ID:
        required: false
      APPLE_ALTOOL_PASSWORD:
        required: false
      WIN_CERT_B64:
        required: false
      WIN_CERT_PASSWORD:
        required: false

# Shared anchors are not supported in GitHub Actions YAML; the per-backend
# jobs below are intentionally near-identical and mostly differ only in the
# backend name, the matrix, and the -D flag passed to CMake.
#
# CMake flag convention (uppercase):  -DAVND_ENABLE_<BACKEND>=ON
#
# Each per-backend job inlines its own SDK fetch (mirroring the proven
# pattern in the `avendish-template/action.yml` composite action) so this
# workflow works for addons whose dependencies.cmake just FetchContents
# avendish (e.g. celtera/avendish-*-processor-template) without any need
# for the ossia/score SDK bundle.
#
# Avendish's CMake defaults every AVND_ENABLE_<X> option to ON, so a job
# that only passes its own enable flag would still build every other
# backend (godot fetches godot-cpp, vst3 fetches the VST3 SDK, etc.) —
# wasted work in every job. Each configure step therefore expands
# ${AVND_OFF} (all backends OFF) BEFORE its own -DAVND_ENABLE_<ITS>=ON;
# the last -D for a given cache variable wins, so only that backend builds.

env:
  # Disable every plugin backend; per-job configure re-enables just one.
  AVND_OFF: >-
    -DAVND_ENABLE_PD=OFF -DAVND_ENABLE_MAX=OFF -DAVND_ENABLE_VST3=OFF
    -DAVND_ENABLE_CLAP=OFF -DAVND_ENABLE_VINTAGE=OFF
    -DAVND_ENABLE_TOUCHDESIGNER=OFF -DAVND_ENABLE_PYTHON=OFF
    -DAVND_ENABLE_GODOT=OFF -DAVND_ENABLE_STANDALONE=OFF
    -DAVND_ENABLE_GSTREAMER=OFF -DAVND_ENABLE_WASM=OFF

jobs:
  # ---------------- setup ----------------
  # Holds each back-end's canonical OS/arch matrix and filters it by the
  # `targets` platform allowlist (a space-separated list of <os-tag>/<arch-tag>
  # triples). Each back-end build job consumes its filtered matrix via
  # `fromJSON(needs.setup.outputs.matrix_<backend>)`. An empty `targets`
  # preserves the previous behavior: every entry runs. This mirrors the
  # setup-job + jq + `$GITHUB_OUTPUT` pattern used by score-addon.yml's
  # builds-dev.yml / builds-sdk.yml / jit.yml.
  setup:
    name: Setup matrices
    runs-on: ubuntu-latest
    outputs:
      matrix_pd: ${{ steps.filter.outputs.matrix_pd }}
      matrix_max: ${{ steps.filter.outputs.matrix_max }}
      matrix_vst3: ${{ steps.filter.outputs.matrix_vst3 }}
      matrix_clap: ${{ steps.filter.outputs.matrix_clap }}
      matrix_vintage: ${{ steps.filter.outputs.matrix_vintage }}
      matrix_touchdesigner: ${{ steps.filter.outputs.matrix_touchdesigner }}
      matrix_python: ${{ steps.filter.outputs.matrix_python }}
      matrix_godot: ${{ steps.filter.outputs.matrix_godot }}
      matrix_standalone: ${{ steps.filter.outputs.matrix_standalone }}
      matrix_gstreamer: ${{ steps.filter.outputs.matrix_gstreamer }}
      matrix_wasm: ${{ steps.filter.outputs.matrix_wasm }}
    steps:
      - name: Compute filtered build matrices
        id: filter
        shell: bash
        env:
          TARGETS: ${{ inputs.targets }}
        run: |
          set -euo pipefail
          # Canonical matrix per back-end. The triple used for filtering is
          # "<os-tag>/<arch-tag>".
          pd='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          max='[
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          vst3='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          clap='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          vintage='[
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          touchdesigner='[
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          python='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          godot='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          standalone='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"},
            {"os":"windows-latest","os-tag":"windows","arch-tag":"x86_64"}
          ]'
          gstreamer='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"},
            {"os":"macos-15","os-tag":"macos","arch-tag":"arm64"}
          ]'
          wasm='[
            {"os":"ubuntu-24.04","os-tag":"linux","arch-tag":"x86_64"}
          ]'

          # Filter one back-end matrix by $TARGETS. Empty targets = keep all.
          filter() {
            local configs="$1"
            if [ -z "${TARGETS// }" ]; then
              jq -c . <<< "$configs"
            else
              jq -c --arg t "$TARGETS" \
                '[ .[] | select( (."os-tag" + "/" + ."arch-tag") as $tr | ($t | split(" ") | map(select(. != ""))) | index($tr) ) ]' \
                <<< "$configs"
            fi
          }

          for backend in pd max vst3 clap vintage touchdesigner python godot standalone gstreamer wasm; do
            echo "matrix_${backend}=$(filter "${!backend}")" >> "$GITHUB_OUTPUT"
          done

  # ---------------- pd ----------------
  pd:
    name: "pd (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.pd && needs.setup.outputs.matrix_pd != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_pd) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 puredata-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install pure-data jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y puredata jq zip ninja
      - name: Checkout PureData headers
        if: runner.os != 'Windows'
        uses: actions/checkout@v4
        with:
          repository: pure-data/pure-data
          path: puredata
      - name: Download Pd Windows binary (provides pd.lib)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          # avendish.pd.cmake does find_library(PD_LIB NAMES pd) on Windows.
          # The official Pd .msw.zip ships src/ headers + bin/pd.lib (the MSVC
          # import lib), unlike the pure-data/pure-data git checkout which is
          # source-only. Mirrors how libossia's ossia-pd provisions pd on Windows.
          PD_VERSION="0.55-2"
          curl -fsSL "http://msp.ucsd.edu/Software/pd-${PD_VERSION}.msw.zip" -o pd.zip
          cmake -E tar xf pd.zip
          echo "PD_WIN_ROOT=$PWD/pd-${PD_VERSION}" >> "$GITHUB_ENV"
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${RUNNER_OS:-}" == "Windows" ]]; then
            PD_INCLUDE="$PD_WIN_ROOT/src"
            PD_LIBARG=(-DCMAKE_LIBRARY_PATH="$PD_WIN_ROOT/bin")
          else
            PD_INCLUDE="$PWD/puredata/src"
            PD_LIBARG=()
          fi
          # macOS ships bash 3.2 which errors on "${empty[@]}" under set -u;
          # the ${arr[@]+"${arr[@]}"} idiom expands to nothing when empty.
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INCLUDE_PATH="$PD_INCLUDE" \
            ${PD_LIBARG[@]+"${PD_LIBARG[@]}"} \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_PD=ON
          cmake --build addon/build --config Release
      # Pd externals are not code-signed, so the CMake package (which also carries
      # the generated <name>-help.pd patches) is just zipped. Falls through to the
      # bash packager when the addon produced no addon/build/package/<name>/.
      - name: Prefer CMake-built package (pd)
        id: cmakepkg
        uses: ossia/actions/package-cmake-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          build-dir: addon/build
          backend: pd
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
      - name: Package pd backend
        id: package
        if: steps.cmakepkg.outputs.zip-path == ''
        uses: ossia/actions/package-pd-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: pd-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.cmakepkg.outputs.zip-path || steps.package.outputs.zip-path }}

  # ---------------- pd-composite ----------------
  # Pure Data via Deken expects a single cross-platform folder containing
  # *.pd_linux + *.pd_darwin + *.dll side-by-side. We unzip every per-OS
  # pd artifact and union their contents into one staging dir.
  pd-composite:
    if: ${{ inputs.pd }}
    needs: pd
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: pd-*
          merge-multiple: false
          path: per-os
      - name: Install zip tools
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 zip unzip
      - name: Merge per-OS Pd artifacts
        shell: bash
        run: |
          set -euo pipefail
          ADDON="${{ github.event.repository.name }}"
          mkdir -p "staging/$ADDON"

          shopt -s nullglob
          ZIPS=( per-os/*/${ADDON}-pd-*.zip )
          if [[ ${#ZIPS[@]} -eq 0 ]]; then
            echo "Error: no per-OS pd artifacts found under per-os/"
            ls -lR per-os || true
            exit 1
          fi

          for z in "${ZIPS[@]}"; do
            dest="unzipped-$(basename "$z" .zip)"
            mkdir -p "$dest"
            unzip -o "$z" -d "$dest"
            # Union every file from <addon>/ into staging/<addon>/.
            if [[ -d "$dest/$ADDON" ]]; then
              cp -Rn "$dest/$ADDON/." "staging/$ADDON/" 2>/dev/null || \
                cp -R "$dest/$ADDON/." "staging/$ADDON/"
            fi
            rm -rf "$dest"
          done

          echo "Composite Pd package contents:"
          find "staging/$ADDON" -maxdepth 3 -print

          (cd staging && zip -r "../${ADDON}-pd-composite.zip" "$ADDON")
          ls -lah "${ADDON}-pd-composite.zip"
      # TODO: optionally produce a .dek package via the Deken CLI
      #   (`pip install hexbinder/deken` or fetch upstream tarball).
      #   The plain zip is enough for v1.
      - uses: actions/upload-artifact@v4
        with:
          name: pd-composite
          path: '*-pd-composite.zip'

  # ---------------- max ----------------
  max:
    name: "max (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.max && needs.setup.outputs.matrix_max != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_max) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Checkout Max SDK
        uses: actions/checkout@v4
        with:
          repository: jcelerier/max-sdk-base
          submodules: recursive
          path: max-sdk-base
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DAVND_MAXSDK_PATH="$PWD/max-sdk-base" \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_MAX=ON
          cmake --build addon/build --config Release
      # If the addon's CMake assembled its own package (the default via
      # avnd_addon_finalize, or an explicit avnd_addon_package call), sign + zip
      # that and skip the bash packager. Backwards-compatible: addons that don't
      # produce addon/build/package/<name>/ fall through to package-max-addon.
      - name: Prefer CMake-built package (max)
        id: cmakepkg
        uses: ossia/actions/package-cmake-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          build-dir: addon/build
          backend: max
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - name: Package max backend
        id: package
        if: steps.cmakepkg.outputs.zip-path == ''
        uses: ossia/actions/package-max-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: max-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.cmakepkg.outputs.zip-path || steps.package.outputs.zip-path }}

  # ---------------- max-composite ----------------
  # Max Packages contain externals/<port>.mxo (macOS bundle) +
  # externals/<port>.mxe64 (Windows). Both OS binaries live side-by-side in
  # the same externals/ folder. We unzip every per-OS max artifact, union
  # externals/ + help/ + extras/ into one staging dir, and re-zip.
  max-composite:
    if: ${{ inputs.max }}
    needs: max
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: max-*
          merge-multiple: false
          path: per-os
      - name: Install zip tools and jq
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 zip unzip jq
      - name: Merge per-OS Max artifacts
        shell: bash
        run: |
          set -euo pipefail
          ADDON="${{ github.event.repository.name }}"
          mkdir -p "staging/$ADDON"/{externals,help,extras}

          shopt -s nullglob
          ZIPS=( per-os/*/${ADDON}-max-*.zip )
          if [[ ${#ZIPS[@]} -eq 0 ]]; then
            echo "Error: no per-OS max artifacts found under per-os/"
            ls -lR per-os || true
            exit 1
          fi

          PKG_INFO_TAKEN=0
          for z in "${ZIPS[@]}"; do
            dest="unzipped-$(basename "$z" .zip)"
            mkdir -p "$dest"
            unzip -o "$z" -d "$dest"

            SRC="$dest/$ADDON"
            if [[ ! -d "$SRC" ]]; then
              echo "Warning: per-OS zip $z had no $ADDON/ root, skipping"
              rm -rf "$dest"
              continue
            fi

            # Union externals/ (preserve .mxo bundle structure with cp -R)
            if [[ -d "$SRC/externals" ]]; then
              cp -R "$SRC/externals/." "staging/$ADDON/externals/" 2>/dev/null || true
            fi
            # Union help/ and extras/ if present
            if [[ -d "$SRC/help" ]]; then
              cp -R "$SRC/help/." "staging/$ADDON/help/" 2>/dev/null || true
            fi
            if [[ -d "$SRC/extras" ]]; then
              cp -R "$SRC/extras/." "staging/$ADDON/extras/" 2>/dev/null || true
            fi
            # Take the first package-info.json + LICENSE/README we see
            if [[ "$PKG_INFO_TAKEN" -eq 0 && -f "$SRC/package-info.json" ]]; then
              cp -f "$SRC/package-info.json" "staging/$ADDON/"
              PKG_INFO_TAKEN=1
            fi
            for f in LICENSE LICENSE.txt LICENSE.md README README.md README.txt; do
              if [[ -f "$SRC/$f" && ! -f "staging/$ADDON/$f" ]]; then
                cp -f "$SRC/$f" "staging/$ADDON/"
              fi
            done

            rm -rf "$dest"
          done

          # Mark composite package as multi-OS in package-info.json
          if [[ -f "staging/$ADDON/package-info.json" ]] && command -v jq >/dev/null 2>&1; then
            jq '.os = ["macos","windows"]' "staging/$ADDON/package-info.json" > /tmp/pi.json
            mv /tmp/pi.json "staging/$ADDON/package-info.json"
          fi

          echo "Composite Max package contents:"
          find "staging/$ADDON" -maxdepth 3 -print

          # -y preserves symlinks (matters for .mxo bundles on macOS)
          (cd staging && zip -ry "../${ADDON}-max-composite.zip" "$ADDON")
          ls -lah "${ADDON}-max-composite.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: max-composite
          path: '*-max-composite.zip'

  # ---------------- vst3 ----------------
  vst3:
    name: "vst3 (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.vst3 && needs.setup.outputs.matrix_vst3 != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_vst3) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 libgl-dev libx11-dev libxcb1-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Checkout VST3 SDK
        uses: actions/checkout@v4
        with:
          repository: steinbergmedia/vst3sdk
          submodules: recursive
          path: vst3sdk
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          EXTRA=()
          # Xcode 26's SDK no longer transitively provides std::terminate to
          # the VST3 SDK's threadchecker_mac.mm.
          if [ "$RUNNER_OS" = "macOS" ]; then
            EXTRA+=( "-DCMAKE_CXX_FLAGS=-include exception" )
          fi
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DVST3_SDK_ROOT="$PWD/vst3sdk" \
            -DSMTG_ENABLE_VSTGUI_SUPPORT=OFF \
            -DSMTG_ADD_VSTGUI=OFF \
            -DSMTG_CREATE_PLUGIN_LINK=OFF \
            -DSMTG_RUN_VST_VALIDATOR=OFF \
            -DSMTG_ENABLE_VST3_PLUGIN_EXAMPLES=OFF \
            -DSMTG_ENABLE_VST3_HOSTING_EXAMPLES=OFF \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_VST3=ON \
            "${EXTRA[@]}"
          cmake --build addon/build --config Release
      - name: Package vst3 backend
        id: package
        uses: ossia/actions/package-vst3-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: vst3-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- clap ----------------
  clap:
    name: "clap (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.clap && needs.setup.outputs.matrix_clap != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_clap) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 libgl-dev libx11-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Checkout CLAP SDK
        uses: actions/checkout@v4
        with:
          repository: free-audio/clap
          path: clap-sdk
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH="$PWD/clap-sdk" \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_CLAP=ON
          cmake --build addon/build --config Release
      - name: Package clap backend
        id: package
        uses: ossia/actions/package-clap-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: clap-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- vintage ----------------
  vintage:
    name: "vintage (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.vintage && needs.setup.outputs.matrix_vintage != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_vintage) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_VINTAGE=ON
          cmake --build addon/build --config Release
      - name: Package vintage backend
        id: package
        uses: ossia/actions/package-vintage-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: vintage-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- touchdesigner ----------------
  touchdesigner:
    name: "touchdesigner (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.touchdesigner && needs.setup.outputs.matrix_touchdesigner != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_touchdesigner) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Checkout TouchDesigner SDK headers
        uses: actions/checkout@v4
        with:
          repository: jcelerier/CustomOperatorSamples
          ref: add-pop-headers
          path: touchdesigner-sdk
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DTOUCHDESIGNER_SDK_PATH="$PWD/touchdesigner-sdk" \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_TOUCHDESIGNER=ON
          cmake --build addon/build --config Release
      # If the addon's CMake assembled its own package (the default via
      # avnd_addon_finalize, or an explicit avnd_addon_package call), sign + zip
      # that and skip the bash packager. Backwards-compatible: addons that don't
      # produce addon/build/package/<name>/ fall through to package-touchdesigner-addon.
      - name: Prefer CMake-built package (touchdesigner)
        id: cmakepkg
        uses: ossia/actions/package-cmake-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          build-dir: addon/build
          backend: touchdesigner
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - name: Package touchdesigner backend
        id: package
        if: steps.cmakepkg.outputs.zip-path == ''
        uses: ossia/actions/package-touchdesigner-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: touchdesigner-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.cmakepkg.outputs.zip-path || steps.package.outputs.zip-path }}

  # ---------------- python ----------------
  python:
    name: "python (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.python && needs.setup.outputs.matrix_python != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_python) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 python3-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Install pybind11
        shell: bash
        run: python -m pip install --upgrade pip pybind11
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DPython_EXECUTABLE="$(python -c 'import sys; print(sys.executable)')" \
            -Dpybind11_DIR="$(python -m pybind11 --cmakedir)" \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_PYTHON=ON
          cmake --build addon/build --config Release
      # If the addon's CMake assembled its own package (the default via
      # avnd_addon_finalize), sign + zip it (it also carries the generated
      # <c_name>_example.py under examples/). Falls through to package-python-addon
      # when addon/build/package/<name>/ is absent.
      - name: Prefer CMake-built package (python)
        id: cmakepkg
        uses: ossia/actions/package-cmake-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          build-dir: addon/build
          backend: python
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - name: Package python backend
        id: package
        if: steps.cmakepkg.outputs.zip-path == ''
        uses: ossia/actions/package-python-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: python-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.cmakepkg.outputs.zip-path || steps.package.outputs.zip-path }}

  # ---------------- godot ----------------
  godot:
    name: "godot (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.godot && needs.setup.outputs.matrix_godot != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_godot) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 libgl-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_GODOT=ON
          cmake --build addon/build --config Release
      # If the addon's CMake assembled its own package (the default via
      # avnd_addon_finalize, or an explicit avnd_addon_package call), sign + zip
      # that and skip the bash packager. Backwards-compatible: addons that don't
      # produce addon/build/package/<name>/ fall through to package-godot-addon.
      - name: Prefer CMake-built package (godot)
        id: cmakepkg
        uses: ossia/actions/package-cmake-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          build-dir: addon/build
          backend: godot
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - name: Package godot backend
        id: package
        if: steps.cmakepkg.outputs.zip-path == ''
        uses: ossia/actions/package-godot-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: godot-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.cmakepkg.outputs.zip-path || steps.package.outputs.zip-path }}

  # ---------------- godot-composite ----------------
  # Godot expects a single addons/<name>/ folder containing the
  # .gdextension config(s) plus a per-platform `bin/<os>-<arch>/` dir.
  # Each per-OS zip already lays this out for one platform; we union them.
  # The .gdextension files reference all platforms regardless, so taking
  # any one of them is fine.
  godot-composite:
    if: ${{ inputs.godot }}
    needs: godot
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: godot-*
          merge-multiple: false
          path: per-os
      - name: Install zip tools
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 zip unzip
      - name: Merge per-OS Godot artifacts
        shell: bash
        run: |
          set -euo pipefail
          ADDON="${{ github.event.repository.name }}"
          mkdir -p "staging/addons/$ADDON/bin"

          shopt -s nullglob
          ZIPS=( per-os/*/${ADDON}-godot-*.zip )
          if [[ ${#ZIPS[@]} -eq 0 ]]; then
            echo "Error: no per-OS godot artifacts found under per-os/"
            ls -lR per-os || true
            exit 1
          fi

          GDEXT_TAKEN=0
          for z in "${ZIPS[@]}"; do
            dest="unzipped-$(basename "$z" .zip)"
            mkdir -p "$dest"
            unzip -o "$z" -d "$dest"

            SRC="$dest/$ADDON"
            if [[ ! -d "$SRC" ]]; then
              echo "Warning: per-OS zip $z had no $ADDON/ root, skipping"
              rm -rf "$dest"
              continue
            fi

            # Take .gdextension config files from the first zip only.
            # All per-OS zips emit identical .gdextension content (rewritten
            # to point at bin/<os>-<arch>/...) — taking one is enough.
            if [[ "$GDEXT_TAKEN" -eq 0 ]]; then
              while IFS= read -r -d '' cfg; do
                cp -f "$cfg" "staging/addons/$ADDON/"
              done < <(find "$SRC" -maxdepth 1 -type f -name '*.gdextension' -print0)
              # Also copy LICENSE/README from the first zip if present
              for f in LICENSE LICENSE.txt LICENSE.md README README.md README.txt; do
                if [[ -f "$SRC/$f" ]]; then
                  cp -f "$SRC/$f" "staging/addons/$ADDON/"
                fi
              done
              GDEXT_TAKEN=1
            fi

            # Union all bin/<os>-<arch>/ folders (each per-OS zip contributes one)
            if [[ -d "$SRC/bin" ]]; then
              cp -R "$SRC/bin/." "staging/addons/$ADDON/bin/" 2>/dev/null || true
            fi

            rm -rf "$dest"
          done

          echo "Composite Godot package contents:"
          find "staging/addons/$ADDON" -maxdepth 4 -print

          (cd staging && zip -r "../${ADDON}-godot-composite.zip" "addons")
          ls -lah "${ADDON}-godot-composite.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: godot-composite
          path: '*-godot-composite.zip'

  # ---------------- standalone ----------------
  standalone:
    name: "standalone (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.standalone && needs.setup.outputs.matrix_standalone != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_standalone) }}
    steps:
      - name: Support longpaths
        if: runner.os == 'Windows'
        shell: bash
        run: git config --global core.longpaths true
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 libgl-dev libx11-dev jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install jq || true
      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: choco install -y jq zip ninja
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${AVND_OFF} \
            -DAVND_ENABLE_STANDALONE=ON
          cmake --build addon/build --config Release
      - name: Package standalone backend
        id: package
        uses: ossia/actions/package-standalone-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
          windows-certificate-base64: ${{ secrets.WIN_CERT_B64 }}
          windows-certificate-password: ${{ secrets.WIN_CERT_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: standalone-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- gstreamer ----------------
  gstreamer:
    name: "gstreamer (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.gstreamer && needs.setup.outputs.matrix_gstreamer != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_gstreamer) }}
    steps:
      - uses: maxim-lobanov/setup-xcode@v1
        if: runner.os == 'macOS'
        with:
          xcode-version: '>=16.4'
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            libglib2.0-dev libjson-glib-dev libsoup2.4-dev pkg-config jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: brew install gstreamer gst-plugins-base json-glib libsoup@2 jq || true
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          GST_LINKARGS=()
          if [[ "${RUNNER_OS:-}" == "macOS" ]]; then
            # Homebrew's pkg-config doesn't always propagate the keg -L path into
            # CMake's PkgConfig:: imported target, so the linker can't find
            # libgstsdp-1.0.dylib etc. Add brew's lib dir + the pkgconfig path
            # explicitly. PKG_CONFIG_PATH covers libsoup@2 (keg-only).
            BREW_PREFIX="$(brew --prefix)"
            export PKG_CONFIG_PATH="$BREW_PREFIX/opt/libsoup@2/lib/pkgconfig:$BREW_PREFIX/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
            GST_LINKARGS=(
              -DCMAKE_MODULE_LINKER_FLAGS="-L$BREW_PREFIX/lib"
              -DCMAKE_SHARED_LINKER_FLAGS="-L$BREW_PREFIX/lib"
              -DCMAKE_EXE_LINKER_FLAGS="-L$BREW_PREFIX/lib"
            )
          fi
          cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            ${GST_LINKARGS[@]+"${GST_LINKARGS[@]}"} \
            ${AVND_OFF} \
            -DAVND_ENABLE_GSTREAMER=ON
          cmake --build addon/build --config Release
      - name: Package gstreamer backend
        id: package
        uses: ossia/actions/package-gstreamer-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
          macos-certificate-base64: ${{ secrets.MAC_CERT_B64 }}
          macos-certificate-password: ${{ secrets.MAC_CERT_PASSWORD }}
          macos-notarize-team-id: ${{ secrets.APPLE_TEAM_ID }}
          macos-notarize-apple-id: ${{ secrets.APPLE_ID }}
          macos-notarize-password: ${{ secrets.APPLE_ALTOOL_PASSWORD }}
      - uses: actions/upload-artifact@v4
        with:
          name: gstreamer-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- wasm ----------------
  wasm:
    name: "wasm (${{ matrix.os-tag }}/${{ matrix.arch-tag }})"
    if: ${{ inputs.wasm && needs.setup.outputs.matrix_wasm != '[]' }}
    needs: setup
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.setup.outputs.matrix_wasm) }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          path: addon
      - name: Install dependencies (Linux)
        shell: bash
        run: |
          sudo apt -y update
          sudo apt install -yqq gcc-14 g++-14 jq zip
          sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-14 100
          sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-14 100
      - name: Setup emsdk
        uses: mymindstorm/setup-emsdk@v14
      - name: Set up Boost
        shell: bash
        run: |
          curl -fsSL https://github.com/ossia/score-sdk/releases/download/sdk31/boost_1_90_0.tar.gz -o boost.tar.gz
          mkdir -p boost/include
          tar -xzf boost.tar.gz --strip-components=1 -C boost/include
      - name: Configure & build
        shell: bash
        run: |
          set -euo pipefail
          emcmake cmake -S addon -B addon/build \
            -DCMAKE_BUILD_TYPE=Release \
            -DBOOST_ROOT="$PWD/boost" \
            -DBoost_INCLUDE_DIR="$PWD/boost/include" \
            -DBoost_NO_BOOST_CMAKE=ON \
            -DCMAKE_POLICY_DEFAULT_CMP0167=OLD \
            -DCMAKE_FIND_ROOT_PATH_MODE_PACKAGE=BOTH \
            -DCMAKE_FIND_ROOT_PATH_MODE_INCLUDE=BOTH \
            -DCMAKE_FIND_ROOT_PATH_MODE_LIBRARY=BOTH \
            ${AVND_OFF} \
            -DAVND_ENABLE_WASM=ON
          cmake --build addon/build --config Release
      - name: Package wasm backend
        id: package
        uses: ossia/actions/package-wasm-addon@master
        with:
          addon-name: ${{ github.event.repository.name }}
          addon-path: addon
          build-dir: addon/build
          output-dir: package
          os-tag: ${{ matrix.os-tag }}
          arch-tag: ${{ matrix.arch-tag }}
      - uses: actions/upload-artifact@v4
        with:
          name: wasm-${{ matrix.os-tag }}-${{ matrix.arch-tag }}
          path: ${{ steps.package.outputs.zip-path }}

  # ---------------- release-continuous ----------------
  release-continuous:
    name: Publish continuous release
    needs: [pd, max, vst3, clap, vintage, touchdesigner, python, godot, standalone, gstreamer, wasm, pd-composite, max-composite, godot-composite]
    if: always() && github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout (for tag setup)
        uses: actions/checkout@v4

      - name: Download all backend artifacts
        uses: actions/download-artifact@v4
        with:
          path: incoming

      - name: Flatten artifacts
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          # actions/download-artifact@v4 creates one dir per artifact; each dir
          # contains exactly one *.zip. Flatten them all into dist/.
          find incoming -type f -name '*.zip' -exec cp -v {} dist/ \;
          echo "Final release payload:"
          ls -lah dist/

      - name: Recreate the 'continuous' release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          # upload-release-action below handles re-uploading assets to an
          # existing tag, but a moving tag with stale assets sometimes confuses
          # GitHub's release UI. Delete the release (but keep the tag floating)
          # before uploading, then svenstaro/upload-release-action will create
          # a fresh prerelease with the correct sha as target.
          if gh release view continuous >/dev/null 2>&1; then
            gh release delete continuous --yes --cleanup-tag || \
              gh release delete continuous --yes || true
          fi

      - name: Upload to continuous prerelease
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag: continuous
          prerelease: true
          overwrite: true
          file_glob: true
          file: dist/*.zip
          target_commit: ${{ github.sha }}
          body: |
            Continuous build from ${{ github.sha }}
            Built: ${{ github.event.head_commit.timestamp }}
